tools:
    external_code_coverage: true
build:
    nodes:
        analysis:
            tests:
                override:
                    - php-scrutinizer-run
    environment:
        php: 8.2.14
filter:
    paths:
        - src/*
